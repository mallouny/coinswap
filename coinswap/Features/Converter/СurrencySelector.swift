import SwiftUI

struct CurrencySelector: View {
    var body: some View {
        VStack(alignment: .leading) {
            HStack {
                Text("текст")
                    .font(type: .light, size: 22)
                    .foregroundStyle(.text)
            }
        }
    }
}
